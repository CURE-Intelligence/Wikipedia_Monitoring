from setuptools import setup, find_packages

setup(
    name='WIKIPEDIA_MONITORING',
    version='0.1',
    packages=find_packages(),  # Automatically find and include all packages
    # install_requires=[]  # Omit this since dependencies are in requirements.txt
)